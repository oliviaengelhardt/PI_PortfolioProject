import "../styles/main.styl";

// const controller = new ScrollMagic.Controller({ globalSceneOptions: { triggerHook: "onEnter" } });
// const tl = new TimelineMax();
// const intro = document.querySelector(".about-text");

// intro.addEventListener("pageshow", onPageShow);
// intro.addEventListener("pagehide", onPageHide);

// function onPageShow() {
    
// }

// tl.to(".about-text", 1, { alpha: 1, ease: Sine.easeOut }, 0)